# Doc assets

- `baseurl_and_api_key.png` — TrueFoundry AI Gateway UI: where to copy the base URL + API key.
- `cli.png` — **placeholder.** Replace with a screenshot of `sudo aitori up --ui` startup output.
- `ui.png` — **placeholder.** Replace with a screenshot of the live-traffic UI (`http://127.0.0.1:9100`) with a few rows.

The two placeholders are flat-color PNGs so the docs render; swap in real screenshots before publishing.
